#!/usr/bin/env bash
# occamy - Claude Code on Accio-Lab Occamy 1.0 requantised by APEX, on the box
# picked at start-up.
#
# Added on 2026-09-26 after the bench compared it. MiniPlus V2.1 on the 32 GB
# box, NanoPlus on the 16 GB one: the full quant spilled out of that card at the
# 262,144 window. Image projector loaded, thinking on and kept across turns as
# its card asks for agent runs (llm-ctl.ps1, llm-ctl-16gb.ps1).
#
# LLM_HOST names the 32 GB box and LLM_HOST_16GB the second one. LLM_CHOICE=1
# or 2 skips the menu, which a run without a terminal requires.

source "$(dirname "${BASH_SOURCE[0]}")/llm-launch.sh"

llm_variant "Occamy sur la machine 32 Go, fenêtre 262k" "${LLM_HOST:-your-32gb-box}" occamy occamy-1.0.apex-i-miniplus '' occamy-1.0 262144
llm_variant "Occamy, version allégée, sur la machine 16 Go, fenêtre 262k" "${LLM_HOST_16GB:-your-16gb-box}" occamy occamy-1.0.apex-i-nanoplus '' occamy-1.0 262144

llm_launch "$@"
